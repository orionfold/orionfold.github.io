---
import FlowDetail from '../FlowDetail.astro';
import detailGrid from '../../../assets/flow/details/detail-grid.webp';
import detailAddFolder from '../../../assets/flow/details/detail-add-folder.webp';
---
<section class="border-t border-border/60 bg-surface-raised/30 px-6 py-14 sm:py-20" aria-labelledby="tour-files">
  <div>
    <div class="mx-auto max-w-3xl text-center">
      <p class="of-eyebrow" data-animate="rise">Your files</p>
      <h3 id="tour-files" class="of-display scroll-mt-28 mt-4 text-[clamp(1.85rem,3.4vw,2.75rem)]" data-animate="lead">No import. No library. No silent overwrite.</h3>
      <p class="of-lede mx-auto mt-5" data-animate="rise">Open ordinary folders and Markdown files. Flow is sandboxed to the folders you choose, refuses name collisions, and sends deleted files to the system Trash. It deliberately does not delete folders.</p>
      <p class="mx-auto mt-5 max-w-2xl leading-relaxed text-text-muted">Your document stays on one primary surface. Open Jobs, Sources, History or Settings in the Workbench beside it, then return to the same file.</p>
    </div>
    <div class="mx-auto mt-12 max-w-4xl" data-animate="plate">
      <FlowDetail src={detailGrid} alt="A Markdown table open in Flow's grid while the underlying file remains plain text." caption="Earlier table-editor detail · The file remains ordinary Markdown" sizes="(min-width: 1024px) 56rem, calc(100vw - 3rem)" />
    </div>
    <FlowDetail class="mx-auto mt-6 max-w-4xl" src={detailAddFolder} alt="Flow's Add Folder sheet: choose one or more folders of Markdown files, the location set to a Flow Projects folder showing its Markdown documents and an assets folder, with Cancel beside Open Folder." caption="Add Folder · Choose one or more folders of Markdown files · Flow sees only what you open" sizes="(min-width: 1024px) 56rem, calc(100vw - 3rem)" />
  </div>
</section>
