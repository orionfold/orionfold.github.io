GIT-HANDOFF.md
OPS-NOTES.md

# Local secrets — OPENROUTER_API_KEY etc. (never commit)
.env.local
.env.*.local

# Edge-function secrets (Stripe rk_/whsec_, etc.) — never commit. Only *.env.example is tracked.
supabase/functions/.env
supabase/functions/*/.env

# Stripe setup notes — sensitive, local-only (never commit)
STRIPE-HANDOFF.md

# Marketing-repo handoff specs — sister `marketing/` workspace drops these here for
# the website session to implement. Local-only by the doc's own preamble; never commit.
MARKETING-HANDOFF.md

# Planning specs + build spine (local-only docs)
_SPECS/
HANDOFF.md

# SEO/AEO audit reports — may contain Search Console / Analytics figures (local-only)
audit-reports/

# Lighthouse CI transient working dir (reports land in audit-reports/lhci/ via the filesystem target)
.lighthouseci/

# Brainstorm / visual-companion scratch (mockups, generated proofs)
.superpowers/
.playwright-mcp/
.cache-imagery/

# Agent config, skills, and session state are local-only tooling — never commit
# (operator policy 2026-05-27, reaffirmed 2026-06-09). The .agents/ tree is the
# Codex-side mirror of .claude/skills/; both stay on disk, out of git.
.claude/
.agents/
.codex/

# Astro / Node build artifacts
node_modules/
node_modules
dist/
.astro/
checks/
.DS_Store
.todos-sync-state.json
_TODOS.json

# Agent instruction files are local-only (2026-06-10) — agents read them from disk
CLAUDE.md
AGENTS.md
CODEX-CC.md
# Top-level underscore paths are local-only by convention (ops/echo files)
/_*
